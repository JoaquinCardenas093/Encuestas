services:
  backend:
    build: ./backend
    restart: unless-stopped
    env_file: .env
    volumes:
      - aurum_data:/data
    expose:
      - "8000"

  web:
    build: ./frontend
    restart: unless-stopped
    depends_on:
      - backend
    ports:
      - "80:80"

volumes:
  aurum_data:
